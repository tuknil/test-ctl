services:
  control-translation:
    build:
      context: .
      dockerfile: Dockerfile
    image: control-translation-service:local
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped
